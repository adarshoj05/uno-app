// TableView.jsx — TEMPORARY PLACEHOLDER
// Real discard-pile/table UI gets built here once roomActions.js / useRoomListener exist.
function TableView({ roomCode }) {
  return <div>Table view placeholder — showing room "{roomCode}"</div>;
}

export default TableView;