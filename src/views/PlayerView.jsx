// PlayerView.jsx — TEMPORARY PLACEHOLDER
// Real hand UI gets built here once roomActions.js / useRoomListener exist.
function PlayerView({ roomCode }) {
  return <div>Player view placeholder — in room "{roomCode}"</div>;
}

export default PlayerView;